/* Importing of Publications : they contain imports for their respective collections. */
import '/imports/api/forum/server/publications.js';
import '/imports/api/accounts/server/publications.js';


/* Idexing text searchable collections
Meteor.startup(function(){
  Meteor.users._ensureIndex({
    "username":"text",
  });
});*/
